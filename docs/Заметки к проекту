После каждого коммита необходимо кратко описывать изменния в данном файле.

Каждый раз, когда ты просишь Cursor добавить фичу, сначала сформулируй запрос в Obsidian (что конкретно нужно), а после генерации — попроси ИИ объяснить новые библиотеки и конструкции. Ты будешь накапливать в своём «втором мозге» не просто код, а осознанные паттерны.

Утро: в Obsidian запиши, что хочешь сделать сегодня (конкретную маленькую фичу).

День: в Cursor через Composer сгенерируй код, прочитай объяснения, протестируй.

Вечер: в Obsidian запиши, что получилось, какие ошибки встретил, что нового узнал. Зафиксируй состояние кода коммитом и пушем.

Так же нужно тут описывать все функции и классы, которые были добавлены или изменены в данном коммите.
Так же нужно тут описывать все ошибки и их решения.
Так же нужно тут описывать все новые возможности и их использование.
Так же нужно тут описывать все новые возможности и их использование.

Внесенные изменения:
- Добавлен файл .gitignore
- Добавлен файл .env
- Добавлен файл .env.example
- Добавлен файл .env.example

Добавлены новые функции:
- Функция add_expense() - добавляет расход в файл
- Функция list_expenses() - выводит список расходов
- Функция show_stats() - выводит статистику по расходам

Ошибки и их решения:
- Ошибка: FileNotFoundError: [Errno 2] No such file or directory: 'expenses.json'
- Решение: Создаем файл expenses.json
- Ошибка: ValueError: invalid literal for int() with base 10: 'expenses.json'
- Решение: Создаем файл expenses.json

команды для запуска программы:
- python main.py add
- python main.py list
- python main.py stats
- python main.py limits set
- python main.py limits list
- python main.py expenses add
- python main.py expenses list
- python main.py expenses stats
- python main.py expenses limits set
- python main.py expenses limits list

Обновили файл main.py:
- Добавлен список критериев для добавления расхода:
    - Сумма должна быть больше нуля.
    - Категория должна быть выбрана из списка.
    - Дата должна быть в формате YYYY-MM-DD.
    - Комментарий должен быть не более 100 символов.
    - Комментарий должен быть не более 100 символов.

    







